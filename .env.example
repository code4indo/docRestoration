# Environment Variables for Docker Compose GAN-HTR
# Copy this file to .env and customize as needed

# ==============================================
# Training Configuration
# ==============================================

# Training script to execute (relative to docRestoration/)
# Options:
#   - scripts/train32_production.sh           (Production FP32 training - DEFAULT ✅)
#   - scripts/train32_smoke_test.sh           (Smoke test - 2 epochs)
#   - dual_modal_gan/scripts/train32.py       (Direct train32.py call)
#   - dual_modal_gan/scripts/train.py         (Mixed precision - legacy)
#   - Your custom script path
TRAINING_SCRIPT=scripts/train32_production.sh

# ==============================================
# GPU Configuration
# ==============================================

# GPU devices to use (comma-separated for multiple GPUs)
# Examples:
#   - CUDA_VISIBLE_DEVICES=0      (Single GPU)
#   - CUDA_VISIBLE_DEVICES=0,1    (Multi GPU)
CUDA_VISIBLE_DEVICES=0

# ==============================================
# HuggingFace Configuration
# ==============================================

# HuggingFace repository for dataset
HF_USERNAME=jatnikonm
HF_REPO_NAME=HTR_VOC

# Enable fast transfer (1=enable, 0=disable)
HF_HUB_ENABLE_HF_TRANSFER=1

# ==============================================
# Port Mapping (change if conflicts)
# ==============================================

# MLflow UI port
MLFLOW_PORT=5001

# TensorBoard port
TENSORBOARD_PORT=6007

# Jupyter port
JUPYTER_PORT=8889

# ==============================================
# Mode (auto-set by docker-compose services)
# ==============================================

# MODE=production      # Full training
# MODE=smoke_test      # Quick validation
# MODE=development     # Interactive development
# MODE=test            # Installation test only
